<% layout('layouts/boilerplate')%>
    <h1>Editing <%=campground.title%>
    </h1>
    <form action="/campgrounds/<%=campground._id%>?_method=PATCH" method="POST">
        <div>
            <label for="title">Title: </label>
            <input type="text" id="title" name="title" value="<%=campground.title%>">
        </div>
        <div>
            <label for="location">Location: </label>
            <input type="text" id="location" name="location" value="<%=campground.location%>">
        </div>

        <button>Submit</button>
    </form>
    <a href="/campgrounds/<%=campground._id%>">Cancel</a>